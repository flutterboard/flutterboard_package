name: flutterboard
description: >-
  Build Flutter widgets from declarative JSON board documents. Early preview
  of the flutterboard engine; APIs are experimental and will change.
version: 0.0.1
repository: https://github.com/flutterboard/flutterboard_package

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
